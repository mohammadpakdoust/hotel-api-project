services:
  - type: web
    name: hotel-api-project
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn hotel_api.wsgi --bind 0.0.0.0:$PORT --timeout 120 --log-file -
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: hotel-db
          property: connectionString
      - key: DEBUG
        value: "True"
      - key: ALLOWED_HOSTS
        value: "*"
      - key: SECRET_KEY
        generateValue: true

databases:
  - name: hotel-db
    plan: free
